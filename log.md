---
title: Operation Log
category: meta
tags: [log, operations]
updated: 2026-08-05
---

# Operation Log

Timestamped record of wiki operations (ingests, updates, exports).

| Timestamp | Operation | Details |
|-----------|-----------|---------|
| 2026-08-05 | INGEST | Ingested `external/code-review-action-items-20260805-1.md` (AI-reviewer code review of `ocbc-cloud-sync` against User Stories v0.6/Detailed Design v0.1/dod.md). Added `deliverables/findings.md` #13 distilling 2 Critical + 5 Required + 3 Optional flagged gaps, noting 2 (callback idempotency, DECOMPRESSING state) are already resolved by same-day code changes to `ocbc-cloud-sync`. Archived source file into `external/.processed/` as `20260805-code-review-action-items-20260805-1.md`. Updated `hot.md`. |
| 2026-08-05 | CODE | Implemented the remaining 8 code-review action items from `deliverables/findings.md` #13 in `ocbc-cloud-sync`: (1) `trace_id` (CS-017) on `Run`/`RunEvent`/schema/`RunResponse`, generated at admission and preserved (fresh per replay); (2) SLA breach detection wired up (`orchestrator.sla.default-deadline-minutes` deadline scheduled at admission, checked on scan tick + transfer-complete callback, `Run.markSlaBreached()`); (3) `RunCancelService` refuses cancellation with `409 TRANSFER_IN_FLIGHT` once `EVENT_PUBLISHED`/`TRANSFERRING`; (4) stale-claim reclaim gated by `orchestrator.scan.stale-claim-threshold-seconds` (default 300s) instead of unconditional reset; (5) `POST /runs` now returns `202 Accepted` + `Location` header instead of `201 Created`; (6)+(9) added 8 new exception-path tests — 7 in a new `ExceptionPathsIT` integration test class plus 1 unit test for DB-down resilience; (7) explicit dispatch guard in `RunTickProcessor` (`RunTaskRepository.existsByRunIdAndStatusIn`) against duplicate task dispatch; (8) `trace_id` added to the `PUBLISH_TRANSFER_EVENT` payload. Updated all affected existing unit/IT tests (`RunTickProcessorTest`, `RunTransferCompletionServiceTest`, `DemoTaskExecutorTest`, `RunAdmissionAndAuthIT`, `OrchestrationFlowIT`) for the new signatures/status codes. Verified via `mvn -Djacoco.skip=false -DskipITs=true verify` — BUILD SUCCESS (128 unit tests, JaCoCo ≥85% branch coverage, Spotless/Checkstyle/PMD all clean; one Spotless javadoc-formatting violation auto-fixed along the way). Updated `README.md` (§2 SLA/trace_id/dispatch-guard/stale-claim narrative, §4 REST API table status codes, §5 new env vars, §7 DoD test counts). Testcontainers ITs (15 total, 7 new) compile cleanly but could not be executed — Docker unavailable in this sandbox; flagged in `README.md` and `findings.md` #13 as pending Docker-equipped verification. Updated `deliverables/findings.md` #13 to Resolved (code). |
| 2026-08-05 | CODE | Added a `dod.md` checklist line noting PMD's `targetJdk=21` static-analysis check, then — per direct instruction to align the project's actual Java version with that DoD line — retargeted `ocbc-cloud-sync` from Java 25 to Java 21 (LTS) as its real build/runtime version: `pom.xml` `java.version` 25→21 (and simplified the now-obsolete "PMD lags behind JDK 25" comments since PMD's `targetJdk=21` now matches the compile target exactly, no longer a workaround); `.github/workflows/ci.yml` JDK setup 25→21; `orchestration-service/Dockerfile` build/runtime base images (`maven:...-eclipse-temurin-25`, `eclipse-temurin:25-jre`) → `-21`; `README.md` Prerequisites and §8 Toolchain notes updated to match; `dod.md`'s Java line reworded from a PMD-workaround note to a plain statement that Java 21 is the actual target. Re-verified via `mvn -Djacoco.skip=false -DskipITs=true verify` — BUILD SUCCESS, all quality gates (tests, JaCoCo, Spotless, Checkstyle, PMD) green under the new Java 21 target (compiled/run using the locally installed Temurin 25 JDK via cross-target compilation — no Java 21 JDK is installed locally, so this could not be verified against an actual Java 21 runtime; CI will use a real JDK 21 per the updated workflow). |
| 2026-08-05 | CODE | Implemented the 2026-08-05 gap-analysis reconciliation deltas in `ocbc-cloud-sync`: added `RunStatus.DECOMPRESSING` and updated `RunStateMachine`/`RunTaskDispatcher`; added `PipelineRegistryEntry.decompressionEnabled` (+ schema/seed); rewrote the transfer-complete callback as a two-phase (`TransferPhase`/`VerificationOutcome`), per-`(run,phase)`-idempotent contract that always returns 200 (`TransferCompleteResponse` with `APPLIED`/`IGNORED` outcome) instead of throwing on an unknown/terminal/wrong-phase run; rewrote `DemoTaskExecutor`'s per-task-type result payloads to match the Detailed Design §5.1 pinned minimal contract (`EXTRACT`, `VALIDATE`, `COMPRESS`, `CLASSIFY_AND_PROMOTE`, `PUBLISH_TRANSFER_EVENT`, plus the shared error envelope on failure). Updated all affected unit/IT tests and `README.md`. Verified via `mvn verify -Djacoco.skip=true -DskipITs=true` — BUILD SUCCESS, 119 unit tests passing, Spotless/Checkstyle/PMD clean (Testcontainers ITs could not run — Docker/Colima unreachable in this environment). Also fixed one unrelated pre-existing Spotless violation blocking `verify`. |
| 2026-08-05 | INGEST | Ingested 4 files from `external/`: `OCBC Data Acquisition - Cloud Sync Detailed Design.md` (2026-08-05 gap-analysis reconciliation, still v0.1), `OCBC Data Acquisition - Cloud Sync User Stories.md` (jumped v0.5→v0.8: caller-supplied-path CS-065–067, object key layout CS-068, allowlisted system IDs CS-069, CS-025 source_id), `OCBC Data Acquisition Platform on AWS.md` (jumped v1.3→v1.5), and `dod.md` (canonical service-level DoD checklist). Created `reference/data-acquisition-platform-v1.5.md` distilling v1.4 (D27 one CMK per AWS account supersedes per-source keys, A18 Diamond Zone per-application buckets, allowlisted source system IDs, caller-supplied-path pipeline mode) and v1.5 (gap-analysis reconciliation: STAGED split into VALIDATED/COMPRESSED, DECOMPRESSING promoted to an audited state, Trigger Lambda task resolution via source_id, manifest-based cloud-side run correlation, push-path compression contradiction removed, source_ref/source_path disambiguated). Updated `reference/data-acquisition-cloud-sync-detailed-design.md` with the reciprocal Detailed-Design-side changes (DECOMPRESSING state §6.1, two-phase transfer-complete callback §8.2, minimal per-task result contract §5.1, manifest-write-last in the push path §5.3, Operations Service unified view + Scheduler Job Adapter schema coupling §7.1, audit≥zone-retention formula §7.2). Updated `reference/cloud-sync-user-stories.md` with version notes for v0.6–v0.8 and a DoD cross-check note. Updated `reference/data-acquisition-platform-v1.3.md` with a forward supersession note. Updated `concepts/kms-byok-key-management.md` (flagged superseded by D27), `concepts/source-registry-and-audit-data-model.md` (pipeline_mode discriminator, source_caller_path_config, source_path/extended dedup, allowlist, manifest compression field), `concepts/sync-push-service-architecture.md` (no-compression confirmed, source_ref/source_path disambiguation, manifest-write-last), and `synthesis/data-acquisition-open-decisions.md` (D27, A18, v1.4/v1.5 update section). Added `deliverables/findings.md` #12 (DoD checklist item mismatch between the canonical `dod.md` and the code repo's own copy — the code repo's copy has an extra CI-security-scan criterion not present in this canonical source). Updated `index.md`, `hot.md`, and `.manifest.json`. Archived source files into `external/.processed/`. |
| 2026-08-03 | DUPLICATE | Found 3 files in `external/FW__IaC_Deployment_Process/` (a "FW:" forward of the same email thread as the earlier "Re:" reply) — confirmed byte-identical via SHA-256 to the files already ingested from `external/Re__IaC_Deployment_Process/` earlier the same day. No new wiki changes made. Archived as duplicates into `external/.processed/` with `20260803-FW__IaC_Deployment_Process-` prefix and recorded in `.manifest.json` with a `duplicate_of` pointer to the original source. |
| 2026-08-03 | INGEST | Ingested 3 files from `external/Re__IaC_Deployment_Process/`: a brand-new "OCBC Data Acquisition — Cloud Sync Detailed Microservice Design" (DRAFT v0.1), plus refreshed copies of the v1.3 platform design and v0.5 Cloud Sync User Stories. Created `reference/data-acquisition-cloud-sync-detailed-design.md`, closing decision D03 with a 5-component decomposition (Orchestrator, Worker, Sync Push Service, Operations Service, Scheduler Job Adapter) on a database-as-task-queue model — superseding the wiki's previously-documented 4-service model and the v1.3 §9.7.2 persistent-lease design. Updated `reference/data-acquisition-platform-v1.3.md` and `reference/cloud-sync-user-stories.md` with the new post-landing decompression capability (Epic H, CS-063/CS-064, resolves Q-15). Updated `concepts/data-onboarding-orchestration-pipeline.md` (new update-callout, decompression step in UC-1 walkthrough) and `synthesis/data-acquisition-open-decisions.md` (DD-01–DD-15 register). Added `deliverables/findings.md` #8 (decomposition-change delivery risk). Updated `index.md`, `hot.md`, and `.manifest.json`. Archived source files into `external/.processed/` with timestamped names. |
| 2026-08-04 | INGEST | Ingested `external/OCBC Data Acquisition - Orchestration Service Code Assessment.md` and `ocbc-data-acquisition/dod.md` (code repo, not part of this vault; directory later renamed from `ocbc-data-acquisition-mini`). Created `reference/orchestration-service-mini-code-assessment.md` (source-document index for the assessment + DoD checklist), `concepts/orchestrator-state-machine-integrity.md` (generalised failure-mode pattern: checkpoint/status coupling, un-acknowledged completions, missing row locking, non-idempotent admission, admission-guard ordering), and `synthesis/orchestration-service-mini-assessment.md` (reconciles the assessment's findings against a subsequent rebuild — most DoD/tooling and admission-ordering/cancel/auth defects fixed, readiness-polling/retry-backoff/business-date-resolution/row-locking gaps remain open by design for this tranche). Updated `concepts/data-onboarding-orchestration-pipeline.md`, `reference/data-acquisition-cloud-sync-detailed-design.md`, and `reference/cloud-sync-user-stories.md` with cross-links. Updated `index.md` and `.manifest.json`. No verbatim reproduction of the proprietary assessment content — paraphrased and attributed as "External: OCBC Data Acquisition - Orchestration Service Code Assessment.md" per the `external/` folder rules. |
| 2026-07-31 | INGEST | Ingested 2 new files from `external/Revised_Data_Acquisition_User_Stories_and_Design_Docs/` (v1.3 platform design MD, superseding v1.2; companion v0.5 Cloud Sync User Stories MD, superseding v0.4). Created `reference/data-acquisition-platform-v1.3.md`. Updated `reference/cloud-sync-user-stories.md`, `concepts/data-onboarding-orchestration-pipeline.md`, `concepts/source-registry-and-audit-data-model.md`, `concepts/dal-security-authentication-and-secrets.md`, `synthesis/data-acquisition-open-decisions.md`, `synthesis/data-acquisition-architecture-overview.md`, `open-questions.md`, and `deliverables/findings.md` (added finding #7: schema-drift/ownership validators deferred to backlog BL-005/BL-006 — a scope reduction, not a documentation gap). Resolved OQ-08 (Control-M scheduler contract, via the new Scheduler Job Adapter) and added OQ-07/OQ-09/OQ-10 to `open-questions.md`. Updated `index.md`, `hot.md`, and `.manifest.json`. Archived source files (2 `.md`, 2 `.docx`, 1 `.zip`) into `external/.processed/` with timestamped names. |
| 2026-07-31 | INGEST | Ingested refreshed Cloud Sync User Stories source from `external/FW__Data_Acquisition_Design_Document/` (markdown export of v0.4 baseline). Updated `reference/cloud-sync-user-stories.md` with version notes and refreshed summary/provenance source list. Updated `hot.md`, `index.md`, and `.manifest.json`. Archived processed source files into `external/.processed/` using timestamped names to avoid collisions. |
| 2026-07-29 | INGEST | Ingested 1 new file from `external/` (`OCBC Data Acquisition Platform on AWS - v1.2.md`). Created 1 new reference page (`reference/data-acquisition-platform-v1.2.md`) capturing v1.2 deltas from v1.1 (workflow-engine removal, operations surface ownership, corrected sync-push auth path, readiness/idempotency/replay semantics). Updated `index.md`, `hot.md`, and `.manifest.json`. Moved source file to `external/.processed/`. |
| 2026-07-27 | INGEST | Ingested 6 files from `external/` (1 PDF LLD, 1 PPTX, 4 PNG diagrams) covering OCBC's Cloud Data Acquisition Service and AI Platform architecture. Created 10 wiki pages (2 entities, 5 concepts, 2 synthesis, 1 reference). Logged 11 open decisions from the source's alignment matrix to `open-questions.md`. Added 2 findings to `deliverables/findings.md` (manual BYOK key rotation cap; orchestration tooling undecided). Created `.manifest.json`. Moved source files to `external/.processed/`. |
| 2026-07-28 | INGEST | Ingested 4 new files from `external/Data_Acquisition_Design_doc/` (v1.1 platform design PDF, 69pp; Cloud Sync User Stories MD, 50 stories; UC-1 narrative DOCX; updated architecture PPTX). Created 5 wiki pages (4 concepts: `temporal-io-workflow-orchestration`, `sync-push-service-architecture`, `source-registry-and-audit-data-model`, `dal-security-authentication-and-secrets`; 3 reference: `data-acquisition-platform-v1.1`, `cloud-sync-user-stories`, `uc1-scheduled-db-poll-narrative`). Updated 7 existing pages (`entities/cloud-data-acquisition-service`, `concepts/data-onboarding-orchestration-pipeline`, `synthesis/data-acquisition-open-decisions`, `synthesis/data-acquisition-architecture-overview`, `reference/data-acquisition-service-lld`, `deliverables/findings`, `open-questions`). Resolved 2 of the 11 original alignment-matrix items (orchestration tooling → Temporal.io+Control-M, D04/D16; non-prod environment → single UAT env, A17) and added the current v1.1 D01–D24 decision register plus 2 remaining outstanding questions (OQ-01, OQ-03). Added 4 new findings (Airflow/diagram staleness refinement, single-non-prod-environment delivery risks x2, dual document-lineage documentation-governance note) and downgraded 1 finding (Control-M/Airflow tension, now largely resolved). Updated `.manifest.json` with 4 new source entries. Moved source files to `external/.processed/` (flattened, 2 Office lock files discarded, empty staging folder removed). |

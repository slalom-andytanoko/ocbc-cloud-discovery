---
title: Findings Register
category: deliverable
tags: [findings, deliverable]
updated: 2026-08-07
cssclass: wide-page
---

# Findings Register

| # | Severity | Finding | Source | Domain | Status | Backlog Ref |
|---|----------|---------|--------|--------|--------|------------|
| 1 | Medium | BYOK KMS keys used for the data acquisition S3 zones do not support automatic AWS-managed rotation; on-demand rotation is manual and capped at 25 rotations per key before a new key must be created and re-aliased. No rotation runbook or rotations-remaining monitoring is currently described. | External: Data Acquisition LLD ([[concepts/kms-byok-key-management]]) | Security / Key Management | Open | — |
| 2 | Low (downgraded from Medium) | **Partially resolved by v1.1.** On-prem/cloud orchestration tooling is now confirmed (Control-M + Temporal.io self-hosted; Apache Airflow explicitly excluded, decision D16). However, the architecture diagrams (`Data Aqusition.png` and `data acquisition.pptx`, both "Unstructured" and "Structured Batch Sources" slides) still depict an Apache Airflow box wrapping the on-prem Production stack — these diagrams have not been redrawn since D16 was decided. Risk: if diagrams are used as a literal build spec (e.g. by an implementation team not reading the latest text decisions), Airflow could be built when it is explicitly not required. | External: OCBC Data Acquisition Platform on AWS - v1.1.pdf ([[concepts/temporal-io-workflow-orchestration]]); External: data acquisition.pptx, Data Aqusition.png ([[entities/cloud-data-acquisition-service]]) | Orchestration / Documentation Consistency | Open (documentation update needed) | — |
| 3 | Low | The architecture diagrams (`Data Aqusition.png` and, reconfirmed by the newly-ingested `data acquisition.pptx`) show two different on-prem service breakdowns: Production uses a 4-service "Cloud Sync Service" (Integration/Control/Security/Orchestration), while Non-Production uses a differently-named 5-service "Cloud Data Acquisition Service" (Integration/Control/Compression/Trigger/KeyCloak) with no explicit Security or Orchestration service and KeyCloak instead of MS Entra ID. v1.1's current six-component model (Gateway, Integration, Control, Security, Orchestration, Sync Push) does not name any of Compression/Trigger/KeyCloak as standalone services, so this is **still unresolved** even after v1.1 — not addressed by the newer design document at all. | External: Data Aqusition.png, data acquisition.pptx ([[entities/cloud-data-acquisition-service]]) | Architecture / Documentation | Open | — |
| 4 | Medium | **Single non-production environment (UAT only, decision A17)** means UAT is the *only* pre-production gate for every release — there is no SIT tier to catch integration issues before UAT, and no environment to load-/soak-test DataSync agent-pool or PostgreSQL sizing at production scale before go-live (v1.1 Appendix E, RSK-01/RSK-03). | External: OCBC Data Acquisition Platform on AWS - v1.1.pdf, Appendix E ([[synthesis/data-acquisition-open-decisions]]) | Environments / Delivery Risk | Open | — |
| 5 | Medium | UAT's databases (Source Registry, Orchestration Audit, and the Temporal persistence store) are hosted in AWS as an interim measure, unlike production's on-premises placement (Option A, chosen specifically to satisfy the D21 outage/resume guarantee). This means **the D21 on-prem-outage-resume behaviour is architecturally unproven in UAT** and can only be validated once production (or a future on-prem UAT) is stood up (v1.1 Appendix E, RSK-05). | External: OCBC Data Acquisition Platform on AWS - v1.1.pdf, §9.7.1 / Appendix E ([[concepts/temporal-io-workflow-orchestration]]) | Reliability / Test Coverage | Open | — |
| 6 | Low | Two independent document lineages describe the same system: the "Cloud Data Acquisition Service — Low Level Design" (v1.0, Liang Chen, 11 Mar 2026) and "OCBC Data Acquisition Platform on AWS" (DRAFT v1.1, AWS-authored, 24 Jul 2026). The two are not explicitly linked as a version chain in either document, despite heavy architectural overlap (same UC-1 use case, same core services). Anyone consulting only the March document risks working from a superseded design (e.g. would not know Airflow is excluded or that a Sync Push Service exists). Recommend OCBC formally retire/relabel the March LLD as historical. | External: Mar 11 _Data-Acquisition-Service-LLD-NarrativeV1.0[63].pdf, OCBC Data Acquisition Platform on AWS - v1.1.pdf ([[reference/data-acquisition-platform-v1.1]]) | Documentation Governance | Open | — |
| 7 | Medium | **Scope reduction, not a documentation gap.** v1.2/CS-053 specified a validation chain that included schema-drift detection and a data-owner (ownership) verification check. v1.3 (31 Jul 2026) formally defers both to the backlog: schema change detection (BL-005, CS-013) and data owner verification (BL-006, CS-057) are not built in the current release. If either is relied upon by downstream consumers or compliance controls before being explicitly re-scoped and delivered, acquired batches could carry undetected schema drift or an unverified data owner. Recommend flagging this deferral explicitly to any stakeholder who assumed these controls were already in scope. | External: OCBC Data Acquisition Platform on AWS - v1.3.md, OCBC Data Acquisition - Cloud Sync User Stories.md v0.5 ([[reference/data-acquisition-platform-v1.3]], [[reference/cloud-sync-user-stories]]) | Data Governance / Scope | Open | BL-005, BL-006 |
| 8 | Medium | **Physical microservice decomposition changed after (potentially) implementation had already started.** [[reference/data-acquisition-cloud-sync-detailed-design]] (DRAFT v0.1, 2026-08-03) formally closes decision D03 with a **five-component** decomposition — Orchestrator, Worker, Sync Push Service, Operations Service, Scheduler Job Adapter — built on a database-as-task-queue model with no persistent lease. This **replaces** the four-service split (Integration/Control/Security/Orchestration Service) this wiki had documented as the working model since v1.1 (24 Jul 2026), including a persistent-lease-with-TTL design in v1.3 §9.7.2 that DD-06 explicitly supersedes. If any implementation team has already built services against the older four-service/lease model, reconciling to the new decomposition (service boundaries, database schema/table-ownership, and the lease-vs-DB-task-queue concurrency mechanism) is a non-trivial rework, not a rename. Recommend confirming with the delivery team whether any code has been built against the superseded model before treating D03 as fully closed for delivery purposes. **Update 2026-08-03: this rework has now been carried out** — `integration-service`'s `FileDropWatcher` was redirected to call orchestration-service's real `/orchestration/runs/initiate` endpoint, the now-orphaned `control-service` MVP stub was deleted entirely (module, docker-compose, gateway routes, Terraform, scripts, docs), and all remaining services were consolidated onto one shared `dataacq` database. Verified via a clean full reactor build and a live smoke test (file-drop → real run → `COMPLETED` status). | External: Re__IaC_Deployment_Process/OCBC Data Acquisition - Cloud Sync Detailed Design.md ([[reference/data-acquisition-cloud-sync-detailed-design]]) | Architecture / Delivery Risk | Resolved (code) | — |
| 9 | High | **Hardcoded plaintext database credential committed in source.** `ocbc-data-acquisition-service/terraform/variables.tf` declares `variable "db_password"` with a plaintext default value (`sensitive = false`) checked into the repository, rather than sourcing the credential from a secrets manager, environment variable, or a `sensitive = true` variable with no default. This is a real, unresolved OWASP-relevant hardcoded-credential exposure against the live RDS instance referenced by `var.db_host` (`ocbc-db.craoma6wm41r.ap-northeast-1.rds.amazonaws.com`) and should be remediated before this repo/state is considered production-ready — ideally by rotating the real RDS password and switching Terraform to pull it from AWS Secrets Manager / SSM Parameter Store. | Code: ocbc-data-acquisition-service/terraform/variables.tf | Security / Secrets Management | Open | — |
| 10 | Medium | **Manual database consolidation required against the real, already-deployed RDS instance.** The Terraform in `ocbc-data-acquisition-service` does not provision the RDS instance itself (no `aws_db_instance` resource) — it only references an existing instance via `var.db_host`. That real instance currently has separate per-service databases (`control_service`, `integration_service`, `security_service`, `orchestration_service`, `sync_push_service`). Code/config in the repo has been updated to point every service at one shared `dataacq` database, but nobody has yet created that database (or migrated/dropped the old ones) on the actual RDS instance — the Terraform ECS deployment will not work correctly against real infra until this manual step is done. | Code: ocbc-data-acquisition-service/terraform/variables.tf, docker-compose.yml | Delivery Risk / Infrastructure | Open | — |
| 11 | Low | **Wiki's root `.processed/` archival folder was not covered by `.gitignore`** despite holding proprietary client documents (e.g. `OCBC Data Acquisition Platform on AWS - v1.1.pdf`). Only `external/` (and, by extension, `external/.processed/`) was ignored; the top-level `.processed/` folder — used as the canonical archive for already-ingested source material per `external/README.md`'s `.processed/` convention — sat untracked in git with no ignore rule, so a `git add .`/`git add -A` would have staged confidential client docs for commit. **Resolved same-day**: added `.processed/` to `.gitignore` and confirmed via `git status` that the folder (now holding 11 files consolidated from `external/.processed/`) no longer appears as trackable. | Repo: `.gitignore`, `.processed/` | Security / Secrets Management | Resolved (code) | — |
| 12 | Low | **Definition-of-Done checklist inconsistency between two sources.** The canonical `dod.md` source document (16-item checklist) does not include a "no Critical/High vulnerabilities in a CI security scan" criterion, but the `ocbc-data-acquisition` code repo's own copy of the checklist (assessed in [[reference/orchestration-service-mini-code-assessment]]) has this extra 17th item. Unclear whether the code repo added an extra local gate, or the canonical `dod.md` is missing an item that should apply engagement-wide. Recommend confirming with the delivery team which version is authoritative before treating either as the single source of truth for release sign-off. | External: dod.md ([[reference/cloud-sync-user-stories]]); Code: ocbc-data-acquisition/dod.md ([[reference/orchestration-service-mini-code-assessment]]) | Documentation Governance / Quality Gates | Open | — |
| 13 | High | **AI-reviewer (Kiro) code review of `ocbc-cloud-sync` against User Stories v0.6/Detailed Design v0.1 flagged 2 Critical + 5 Required + 3 Optional gaps.** All 10 items are now resolved in code as of 2026-08-05: the 2 original Critical items (idempotent transfer-complete redelivery handling; missing `DECOMPRESSING` state) were fixed earlier the same day, and the remaining 8 code-review action items have now also been implemented and verified: (1) `trace_id` (CS-017) added to `Run`/`RunEvent`/`db/schema.sql`/`RunResponse`, generated once at admission and preserved (fresh) across replays; (2) SLA breach detection wired up — every admitted run is scheduled an SLA deadline (`orchestrator.sla.default-deadline-minutes`), checked on every scan tick and transfer-complete callback, flips `Run.markSlaBreached()`; (3) `RunCancelService` now refuses cancellation with `409 TRANSFER_IN_FLIGHT` once a run has reached `EVENT_PUBLISHED`/`TRANSFERRING`; (4) stale-claim reclaim now gated by `orchestrator.scan.stale-claim-threshold-seconds` (default 300s) instead of resetting unconditionally every tick; (5) `POST /runs` now returns `202 Accepted` with a `Location` header instead of `201 Created`; (6) all 8 exception-path acceptance criteria now have test coverage — 7 as a new `ExceptionPathsIT` integration test class (redelivery-to-completed, unknown-run callback, cancel-during-transfer, malformed `batch_date`, SLA-breach-while-queued, resume-on-CANCELLED, replay-while-active) plus 1 unit test for DB-down resilience (`RunAdmissionServiceTest#propagatesRepositoryFailureWhenDatabaseIsUnavailable`); (7) `RunTickProcessor` now has an explicit dispatch guard (`RunTaskRepository.existsByRunIdAndStatusIn`) preventing a second task dispatch while one is already `PENDING`/`CLAIMED`; (8) `trace_id` is now included in the `PUBLISH_TRANSFER_EVENT` payload. Full `mvn -Djacoco.skip=false -DskipITs=true verify` passes (128 unit tests, JaCoCo >=85% branch coverage, Spotless, Checkstyle, PMD all green); README.md updated to document all 8 behaviours. **Caveat**: the 7 new `ExceptionPathsIT` tests compile cleanly but have not actually been executed — this sandbox has no local Docker/Testcontainers runtime — so they should be run on a Docker-equipped machine or in CI before this finding is closed out entirely. | External: code-review-action-items-20260805-1.md | Code Quality / Delivery Risk | Resolved (code) - IT execution pending Docker-equipped verification | - |
| 14 | Medium | **Follow-up AI-reviewer (Kiro) code review of `ocbc-cloud-sync` (2026-08-06) confirms all 10 findings from #13 are resolved, then flags 6 new Required + 1 nit + 2 optional findings.** Required: (1) SLA breach detection has no per-pipeline override, so CS-027's "no deadline configured -> report as a config gap" exception path can't be satisfied; (2) resume (CS-040) doesn't check whether the staged batch still exists before allowing resume; (3) replay (CS-041) doesn't guard against a second concurrent replay for the same pipeline+batch-date; (4) no per-caller rate limiting (CS-038); (5) the SLA deadline scheduled at admission isn't recorded as its own audit event; (6) all 21 unit test files are missing `@DisplayName`, and existing integration-test `@DisplayName`s reference `UC-x` instead of the canonical `CS-xxx` story IDs. Nit: `RunCancelService` runs two lookup queries instead of one `status IN (...)` query. Optional: the transfer-complete `IGNORED` callback path can silently drop a newly-set SLA-breach flag; `RunReplayService` doesn't schedule an SLA deadline on replay runs. **All 9 items fixed same-day**: added `PipelineRegistryEntry.slaDeadlineMinutes` (optional per-pipeline override, WARN-logged fallback to the environment default); added a pipeline+batch-date active-run guard to `RunReplayService`; documented the staged-data resume check as a known limitation (README known-limitations section, pending a real staging connector); added a simple in-memory per-API-key rate limiter; added `RunEventType.SLA_SCHEDULED` recorded at admission and on replay; added `@DisplayName` to all unit test methods and standardised integration-test `@DisplayName`s on `CS-xxx` IDs; combined `RunCancelService`'s two stand-down queries into one `status IN (...)` query; moved the SLA-breach check ahead of the status branch in `RunTransferCompletionService` so it's persisted on the `IGNORED` path too; `RunReplayService` now schedules a fresh SLA deadline on every replay. Verified via full reactor `mvn verify` (unit + integration tests, JaCoCo/Spotless/Checkstyle/PMD all green). Committed to `ocbc-cloud-sync` on branch `Tranches-2` as 2 logical commits (not pushed). **Correction 2026-08-06**: the `@DisplayName`/CS-xxx sweep (item 6) was not actually complete as first claimed — a manual spot-check found 1 test entirely missing `@DisplayName` and 12 more test files missing a `CS-xxx`/`DD-xx` tag. A rigorous re-scan (regex spanning `@Test` through the method signature, checking both annotation orders) confirmed 0 remaining missing `@DisplayName`s and left only the intentionally-exempt pure config/contract/POJO-identity tests without a story ID. Also retitled `steering/design.md`'s header (a prior edit had stripped it to just `# OCBC Data Acquisition`) to `# OCBC Data Acquisition — Orchestration Service Design (Tranche 2)`. Re-verified via full reactor `mvn verify` — BUILD SUCCESS. Committed as 2 further logical commits on `Tranches-2` (not pushed). | External: code-review-findings-20260806-1.md ([[reference/orchestration-service-code-review-20260806]]) | Code Quality / Delivery Risk | Resolved (code) | - |
| 15 | Medium | **Fourth AI-reviewer (Kiro) code review of `ocbc-cloud-sync` (2026-08-07) — the connector-story pass (CS-006/007/008) — confirms all 9 findings from #14 are resolved, then flags 7 Required + 1 nit + 2 optional findings, most of them accepted tranche-scope deferrals rather than defects.** Required: (1) SLA breach is a boolean flag, not the distinct `SLA_BREACH` terminal status the Detailed Design §6.1/§8.2 state machine shows [accepted deviation — design-owner decision]; (2) a CS-006 permission-denied source read (`AccessDeniedException`) is misclassified as `EXTRACT_FAILED`/`TRANSIENT` instead of `SOURCE_FILE_ACCESS_DENIED`/`PERMANENT` naming the file [fixed]; (3) CS-007 has no S3 source connector, ETag/version pre-read check, or `AbortMultipartUpload` (single-part PUT only) [deferred — future tranche]; (4) CS-008 Parquet landing is not implemented, depends on CS-004 (JDBC/Oracle connector) [deferred]; (5) CS-020 `batch_date_resolved` is absent from the `POST /runs` 202 response, business-date resolution (Epic G) not implemented [deferred]; (6) two `WorkerExtractFlowIT` tests lack `@DisplayName` [fixed]; (7) the CS-026 transfer-complete callback doesn't capture `bytes_transferred`/`files_transferred`/`datasync_execution_arn` metrics [fixed]. Nit: `ApiKeyAuthFilter`'s per-key rate-limit map grows without eviction [fixed]. Optional: `OrchestrationFlowIT` javadoc references deprecated `UC-x` IDs [fixed — CS-021/CS-022]; the API-key guardrail remains a stand-in for Spring Cloud Gateway + Entra ID token validation [deferred]. **All five actionable code fixes (#2,#6,#7,#8,#9) implemented same-day** and verified via full reactor `mvn verify` (unit + Testcontainers ITs, JaCoCo ≥85%, Spotless/Checkstyle/PMD green): the wrapped-`AccessDeniedException` classification + a deterministic test, nullable `bytes/files/datasyncExecutionArn` on `TransferCompleteRequest` persisted on the `run` row (+ schema.sql columns, `Run.recordTransferMetrics`, `TransferMetrics` record), rate-limit stale-window eviction, the two `@DisplayName`s, and the UC-x javadoc rewording. The five accepted deviations (#1,#3,#4,#5,#10) were recorded as known limitations in the repo README and `steering/design.md`. All ten findings, plus the three prior reviews, were then codified into a new steering register `steering/design-decisions-and-guardrails.md`, which pairs each finding with the concrete recurrence guardrail (a named test, schema rule, or lint/grep gate) and marks the accepted deviations "do not re-flag". Committed on branch `Tranches-2` (not pushed). | External: code-review-findings-20260807-1.md ([[reference/orchestration-service-code-review-20260807]]) | Code Quality / Delivery Risk | Resolved (code) | - |
| 16 | Low | **Readiness polling has no timeout/window bound (CS-021); `ocbc-cloud-sync` `design.md` §5 also mis-described the mechanism (corrected 2026-08-07).** Scoped to Tranche 1 & 2 (branch `Tranches-2`). A `DB_POLL` run whose source never becomes ready re-dispatches `CHECK_READINESS` indefinitely: `RunTickProcessor.progressSettledTask()` reschedules `next_wake_at` to `readiness-poll-interval-seconds` on each not-ready result, but there is no poll-window expiry or give-up outcome. The only deadline mechanism, `RunSlaBreachEvaluator`, merely sets the `slaBreached` flag — it never fails or stops the run, so a never-ready run never terminates. CS-021's "SLA alert on window expiry" is unimplemented (deliberately, as readiness is simulated this tranche). Separately, `steering/design.md` §5 previously claimed readiness had "no re-dispatch mechanism" and that `readiness-poll-interval-seconds` was "not yet wired" — inaccurate, since the code does re-dispatch on that interval; the §5 wording was corrected on 2026-08-07 to state the missing piece is the timeout bound, not the loop. Connector-story deferrals in the same tranche (CS-007 S3 source/ETag/multipart, CS-008 Parquet, both blocked on later tranches) remain tracked in finding #15. | Code: ocbc-cloud-sync `RunTickProcessor`/`RunProgressionService`/`RunSlaBreachEvaluator`, steering/design.md §5 ([[synthesis/orchestration-service-mini-assessment]]) | Orchestration / State-Machine Correctness | Open (doc corrected; timeout deferred to real-readiness tranche) | CS-021 |
| 17 | Medium | **CS-004 (JDBC/Oracle connector) implementation spec produced; build in progress.** Implementation specification created at [[reference/cs-004-jdbc-oracle-connector-implementation]]. Covers `OracleSourceConnector` (bounded-memory streaming via `setFetchSize`, S3 multipart write, SHA-256 manifest, CS-019 byte ceiling), `HikariCP` per-source pool factory (DD-13), Worker `WorkerTaskExecutor` RELATIONAL branch, schema additions, full unit + Testcontainers IT plan (`OracleExtractFlowIT` against `gvenzl/oracle-xe` + LocalStack), and error classification (TRANSIENT/PERMANENT per A4). **Accepted deviation:** CSV interim format pending Q-16 Oracle→Parquet mapping sign-off (CS-008 dependency); to be recorded as `DD-B-CS004-CSV-INTERIM` in `steering/design-decisions-and-guardrails.md`. CS-008 (Parquet landing) unblocked once this connector and Q-16 are both delivered. | [[reference/cs-004-jdbc-oracle-connector-implementation]] | Code Quality / Delivery | Open (spec done; build + IT execution pending) | CS-004, CS-008 |

